# 기본 환경 변수 설정
# 이 파일은 모든 환경(개발/프로덕션)에서 공통으로 사용되는 환경 변수의 템플릿입니다.

# Supabase 인증 관련 설정
# Supabase 프로젝트 설정에서 찾을 수 있습니다
NEXT_PUBLIC_SUPABASE_URL="your-project-url"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# OAuth 리다이렉션 URL 설정
# 개발 환경: http://localhost:3000
# 프로덕션 환경: https://your-project-name.vercel.app
NEXT_PUBLIC_OAUTH_REDIRECT_URL="your-oauth-redirect-url" 